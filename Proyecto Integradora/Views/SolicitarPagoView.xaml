<Page x:Class="Proyecto_Integradora.Views.SolicitarPagoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:Proyecto_Integradora.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="900"
      Title="SolicitarPagoView">

    <Page.DataContext>
        <vm:SolicitarPagoViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                Grid.Column="2" 
                                Focusable="False" 
                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                ClickMode="Press" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentPresenter" 
                                IsHitTestVisible="False" 
                                Content="{TemplateBinding SelectionBoxItem}" 
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                Margin="{TemplateBinding Padding}" 
                                VerticalAlignment="Center"/>
                            <Popup x:Name="Popup" 
                                Placement="Bottom" 
                                AllowsTransparency="True" 
                                IsOpen="{TemplateBinding IsDropDownOpen}" 
                                PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" 
                                    B
                          Style="{StaticResource DarkComboBoxStyle}">
                    <ComboBox.Resources>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="#1A1A1A"/>
                            <Setter Property="Padding" Value="8,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border x:Name="Border" 
                                            Background="{TemplateBinding Background}" 
                                            Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#D32F2F"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#D32F2F"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setterigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="#0A0A0A">
        <!-- Logo de fondo con baja opacidad -->
        <Image Source="/Core/fenix_logo.png" Opacity="0.03" Stretch="Uniform" Margin="50"/>

        <Grid Margin="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- SECCIÓN IZQUIERDA: CONFIGURACIÓN -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,40,0">
                <TextBlock Text="Nueva Solicitud" FontSize="28" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Configura los detalles de tu crédito a continuación." Foreground="#888" Margin="0,0,0,30"/>
                <TextBlock Text="{Binding LimiteCredito, StringFormat=Límite disponible: ${}{0:N2}}" Foreground="#55D6A9" Margin="0,0,0,10"/>

                <!-- MONTO -->
                <Label Content="Monto a solicitar:" Foreground="#E5E5E5" FontSize="14" Margin="0,10,0,5"/>
                <DockPanel LastChildFill="True">
                    <TextBlock Text="{Binding Monto, StringFormat={}{0:C0}}" 
                               Foreground="#FF3131" FontWeight="Bold" FontSize="18" 
                               DockPanel.Dock="Right" Width="100" TextAlignment="Right"/>
                    <Slider x:Name="MontoSlider" 
                            Value="{Binding Monto, Mode=TwoWay}"
                            Minimum="1" Maximum="{Binding LimiteCredito}" 
                            TickFrequency="500" IsSnapToTickEnabled="True" 
                            VerticalAlignment="Center" Cursor="Hand"/>
                </DockPanel>

                <!-- PLAZO -->
                <Label Content="Plazo de pago (Meses):" Foreground="#E5E5E5" FontSize="14" Margin="0,25,0,5"/>
                <ComboBox x:Name="PlazoCombo" 
                          SelectedValuePath="Tag"
                          SelectedValue="{Binding Plazo, Mode=TwoWay}"
                          Height="35" Background="#1A1A1A" BorderBrush="#333" Foreground="White" Padding="10,5">
                    <ComboBox.Resources>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="#1A1A1A"/>
                            <Setter Property="Padding" Value="5,8"/>
                        </Style>
                    </ComboBox.Resources>
                    <ComboBoxItem Content="3 Meses" Tag="3" IsSelected="True"/>
                    <ComboBoxItem Content="6 Meses" Tag="6"/>
                    <ComboBoxItem Content="12 Meses" Tag="12"/>
                    <ComboBoxItem Content="24 Meses" Tag="24"/>
                </ComboBox>

                <!-- TÉRMINOS -->
                <CheckBox x:Name="CheckTerminos" 
                          Content="Acepto los términos y condiciones" 
                          Foreground="#AAA" Margin="0,40,0,10" 
                          Cursor="Hand" VerticalContentAlignment="Center"/>

                <!-- BOTÓN ENVIAR -->
                <Button Content="ENVIAR SOLICITUD" 
                        Command="{Binding EnviarCommand}"
                        IsEnabled="{Binding IsChecked, ElementName=CheckTerminos}"
                        Height="45" Background="#FF3131" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <!-- SECCIÓN DERECHA: RESUMEN (TARJETA) -->
            <Border Grid.Column="1" Background="#151515" CornerRadius="15" Padding="25" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.5"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Resumen de Pago" Foreground="#FF3131" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,20"/>

                    <DockPanel Margin="0,10">
                        <TextBlock Text="Tasa de interés" Foreground="#888"/>
                        <TextBlock Text="5.0% Anual" Foreground="White" HorizontalAlignment="Right"/>
                    </DockPanel>

                    <Separator Background="#333" Margin="0,10"/>

                    <TextBlock Text="Pago Mensual Estimado:" Foreground="#888" Margin="0,15,0,5"/>
                    <TextBlock Text="{Binding PagoMensual, StringFormat={}{0:C2}}" 
                               Foreground="White" FontSize="36" FontWeight="Bold"/>

                    <TextBlock Text="Total a devolver:" Foreground="#888" Margin="0,20,0,5"/>
                    <TextBlock Text="{Binding TotalDevolver, StringFormat={}{0:C2}}" 
                               Foreground="#AAA" FontSize="18" FontWeight="SemiBold"/>

                    <Border Background="#251010" CornerRadius="8" Margin="0,30,0,0" Padding="15">
                        <TextBlock Text="* Los valores mostrados son informativos y pueden variar según el perfil del cliente." 
                                   Foreground="#A55" FontSize="11" TextWrapping="Wrap" TextAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>