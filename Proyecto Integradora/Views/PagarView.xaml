<Page x:Class="Proyecto_Integradora.Views.PagarView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:vm="clr-namespace:Proyecto_Integradora.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="900"
      Title="PagarView">

        <Page.DataContext>
                <vm:PagarViewModel/>
        </Page.DataContext>

    <Grid Background="#0A0A0A">
                <Image Source="/Core/fenix_logo.png" Opacity="0.03" Stretch="Uniform" Margin="50"/>

        <Grid Margin="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Stretch" Margin="0,0,40,0">
                <TextBlock Text="Mis Vales" FontSize="28" Foreground="White" FontWeight="Bold" Margin="0,0,0,8"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Button Content="Todos" Command="{Binding VerTodosCommand}" Height="30" Margin="0,0,8,0" Background="#2A2A2A" Foreground="White" BorderBrush="#444"/>
                    <Button Content="Pendientes" Command="{Binding VerPendientesCommand}" Height="30" Margin="0,0,8,0" Background="#2A2A2A" Foreground="White" BorderBrush="#444"/>
                    <Button Content="Aceptados" Command="{Binding VerAceptadosCommand}" Height="30" Margin="0,0,8,0" Background="#2A2A2A" Foreground="White" BorderBrush="#444"/>
                    <Button Content="Rechazados" Command="{Binding VerRechazadosCommand}" Height="30" Background="#2A2A2A" Foreground="White" BorderBrush="#444"/>
                </StackPanel>

                <DataGrid AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          ItemsSource="{Binding MisVales}"
                          SelectedItem="{Binding ValeSeleccionado, Mode=TwoWay}"
                          Background="Transparent"
                          RowBackground="#151515"
                          AlternatingRowBackground="#1A1A1A"
                          Foreground="White"
                          BorderBrush="#333"
                          BorderThickness="1"
                          GridLinesVisibility="None"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          Height="350">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#111"/>
                            <Setter Property="Foreground" Value="#AAA"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding id}" Width="170"/>
                        <DataGridTextColumn Header="Monto" Binding="{Binding montoSolicitado, StringFormat=C}" Width="120"/>
                        <DataGridTextColumn Header="Restante" Binding="{Binding montoRestante, StringFormat=C}" Width="120"/>
                        <DataGridTextColumn Header="Plazo" Binding="{Binding plazoPagoMeses, StringFormat='{}{0} meses'}" Width="90"/>
                        <DataGridTextColumn Header="Estatus" Binding="{Binding status}" Width="100"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding createdAt, StringFormat=d}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <Border Grid.Column="1" Background="#151515" CornerRadius="15" Padding="25" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.5"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Realizar Pago" Foreground="#FF3131" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,20"/>

                    <TextBlock Text="Vale seleccionado:" Foreground="#888" Margin="0,10,0,5"/>
                    <TextBlock Text="{Binding ValeSeleccionado.id}" Foreground="White" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap"/>

                    <TextBlock Text="Monto restante:" Foreground="#888" Margin="0,20,0,5"/>
                    <TextBlock Text="{Binding ValeSeleccionado.montoRestante, StringFormat=C}" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,0,0,20"/>

                    <Separator Background="#333" Margin="0,0,0,20"/>

                    <Label Content="Monto a pagar:" Foreground="#E5E5E5" FontSize="14" Margin="0,0,0,5" Padding="0"/>
                    <TextBox Text="{Binding MontoPago, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="40" Background="#1A1A1A" BorderBrush="#333" Foreground="White" Padding="10,8" Margin="0,0,0,30"/>

                    <Button Content="PAGAR VALE" Height="50" Background="#FF3131" Foreground="White" FontWeight="Bold" FontSize="16" BorderThickness="0" Cursor="Hand" Command="{Binding PagarValeCommand}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="15" Color="#FF3131" Opacity="0.3" ShadowDepth="0"/>
                        </Button.Effect>
                    </Button>

                    <TextBlock Text="El pago usa el id del vale seleccionado para llamar /api/Vales/{valeId}/Pagar." 
                               Foreground="#555" FontSize="12" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>