<Page x:Class="Proyecto_Integradora.Views.PagarView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="900"
      Title="PagarView">

    <Grid Background="#0A0A0A">
        <Image Source="/Core/fenix logo.png" Opacity="0.03" Stretch="Uniform" Margin="50"/>

        <Grid Margin="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Stretch" Margin="0,0,40,0">
                <TextBlock Text="Estado de Cuenta" FontSize="28" Foreground="White" FontWeight="Bold" Margin="0,0,0,5"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
                    <TextBlock Text="Estado actual:" Foreground="#888" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Border Background="#251010" BorderBrush="#FF3131" BorderThickness="1" CornerRadius="12" Padding="15,4">
                        <TextBlock Text="Próximo a Vencer" Foreground="#FF3131" FontWeight="SemiBold" FontSize="12"/>
                    </Border>
                </StackPanel>

                <Label Content="Próximas Cuotas:" Foreground="#E5E5E5" FontSize="14" Margin="0,10,0,10"/>

                <DataGrid x:Name="CuotasGrid" AutoGenerateColumns="False" HeadersVisibility="Column" 
                          Background="Transparent" RowBackground="#151515" AlternatingRowBackground="#1A1A1A"
                          Foreground="White" BorderBrush="#333" BorderThickness="1" GridLinesVisibility="None" 
                          CanUserAddRows="False" Height="250">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#111"/>
                            <Setter Property="Foreground" Value="#AAA"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="No." Binding="{Binding NumeroCuota}" Width="50"/>
                        <DataGridTextColumn Header="Vencimiento" Binding="{Binding Fecha}" Width="*"/>
                        <DataGridTextColumn Header="Monto" Binding="{Binding Monto}" Width="*"/>
                        <DataGridTextColumn Header="Estatus" Binding="{Binding Estatus}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Button Content="Descargar Historial de Pagos" Background="Transparent" Foreground="#888" 
                        BorderBrush="#333" BorderThickness="1" Height="35" Margin="0,20,0,0" Cursor="Hand" Width="200" HorizontalAlignment="Left">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <Border Grid.Column="1" Background="#151515" CornerRadius="15" Padding="25" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.5"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Realizar Pago" Foreground="#FF3131" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,20"/>

                    <TextBlock Text="Monto a pagar hoy:" Foreground="#888" Margin="0,10,0,5"/>
                    <TextBlock Text="$1,250.00" Foreground="White" FontSize="42" FontWeight="Bold" Margin="0,0,0,20"/>

                    <Separator Background="#333" Margin="0,0,0,20"/>

                    <Label Content="Método de Pago:" Foreground="#E5E5E5" FontSize="14" Margin="0,0,0,5" Padding="0"/>
                    <ComboBox x:Name="MetodoPagoCombo" Height="40" Background="#1A1A1A" BorderBrush="#333" Foreground="White" Padding="10,8" Margin="0,0,0,30">
                        <ComboBoxItem Content="Tarjeta de Crédito / Débito" IsSelected="True"/>
                        <ComboBoxItem Content="Transferencia SPEI"/>
                        <ComboBoxItem Content="Pago en Efectivo (OXXO)"/>
                    </ComboBox>

                    <Button Content="PAGAR AHORA" Height="50" Background="#FF3131" Foreground="White" FontWeight="Bold" FontSize="16" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="15" Color="#FF3131" Opacity="0.3" ShadowDepth="0"/>
                        </Button.Effect>
                    </Button>

                    <TextBlock Text="El pago se reflejará de forma inmediata." 
                               Foreground="#555" FontSize="12" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>